import Head from "next/head"

import Layout from '../components/Layout'

export default () => (
    <Layout>
        <Head>
            <title>Timeau - Contact Us</title>
            <meta name="description" content="Contact Us" />
        </Head>

    </Layout>
)
